# Prometheus scrape config for your application metrics endpoint.
PROMETHEUS_SCRAPE_TARGET=host.docker.internal:3001
PROMETHEUS_JOB_NAME=application-api

# Published host ports.
GRAFANA_PORT=3003
PROMETHEUS_PORT=9090
LOKI_PORT=3100
JAEGER_UI_PORT=16686
JAEGER_OTLP_HTTP_PORT=4318

# Retention settings.
PROMETHEUS_RETENTION=15d
LOKI_RETENTION=168h

# Grafana credentials.
# Local development: defaults below are fine.
# Production: set GRAFANA_ADMIN_USER and GRAFANA_ADMIN_PASSWORD as GitHub secrets
#             — the deploy workflow injects them into .env automatically.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Contract values for the monitored application.
# Configure these values in your app runtime environment.
LOKI_URL=http://localhost:3100
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
